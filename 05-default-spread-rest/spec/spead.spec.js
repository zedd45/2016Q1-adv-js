'use strict';

// require something here


describe('Spread Operator', function () {

    it('allows an expression to be expanded in places where multiple arguments (for function calls) or multiple elements (for array literals) are expected');
    it('can replace `apply` in some use cases');
    it('can assemble multiple arrays inline');
    it('will convert "array like" values to arrays');
});
